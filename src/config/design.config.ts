import type { DesignConfig } from "./types";

/** Figma poster artboard — all component tokens are px at this resolution. */
export const designConfig: DesignConfig = {
  artboardWidth: 1920,
  artboardHeight: 1080,
};
