import type { RandomnessConfig } from "./types";

export const randomnessConfig: RandomnessConfig = {
  seed: 2026,
  positionJitter: 1,
  sizeJitter: 1,
  angleJitter: 1,
  enableRandomVariation: true,
  eventMinuteMin: 8,
  eventMinuteMax: 85,
};
