/** Set to `false` to revert homepage cards to kickoff gradient covers only. */
export const HOME_ARTWORK_THUMBNAILS = true;

/** Scale for homepage card artwork — lower shows more of both team halves. */
export const HOME_THUMBNAIL_FIT = 0.58;
