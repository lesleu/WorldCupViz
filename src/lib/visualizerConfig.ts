/**
 * @deprecated Import from `@/config` instead.
 * Re-exported for backward compatibility — no runtime behavior change.
 */
export type { VisualizerConfig } from "@/config/types";
export { VISUALIZER_CONFIG, cfg } from "@/config";
